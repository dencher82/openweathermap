import React from 'react';
import Weather from "./Weather";
import Form from "./Form"; // type 'rsc'

const Data = () => {
    return (
        <div>
            <Form/>
            <Weather/>
        </div>
    );
};

export default Data;